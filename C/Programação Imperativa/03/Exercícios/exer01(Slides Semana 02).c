#include <stdio.h>

int main() {

      int num1, num2, soma;

      printf("Type two integers:\n");
      scanf("%d %d", &num1, &num2);
      soma = num1 + num2;
      printf("The sum is: %d", soma);

      return 0;
}
